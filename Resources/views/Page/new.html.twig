{% extends 'AzhuroPageBundle::layout.html.twig' %}

{% block container %}
    <h1>Page creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('azhuro_page_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
